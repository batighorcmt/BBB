<?php

// Placeholder auth routes file to prevent missing include errors.
// Replace with your actual auth routes (Breeze/Jetstream/Fortify) as needed.

use Illuminate\Support\Facades\Route;

// Minimal login route placeholder to satisfy redirects.
Route::get('/login', function () {
    return 'Login page placeholder';
})->name('login');
